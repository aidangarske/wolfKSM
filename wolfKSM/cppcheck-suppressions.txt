# wolfKSM cppcheck suppression file
# Suppress false positives and system-level checks

# System include warnings (not our code)
missingIncludeSystem

# wolfSSL headers may trigger warnings we can't control
unmatchedSuppression

# Suppress only external wolfssl headers (not our code)
*:/usr/include/wolfssl/*
*:/usr/local/include/wolfssl/*
